import UploadCard from "@/components/UploadCard";

export default function VideoUpload() {
  return (
    <div className="container min-w-screen max-w-none min-h-screen flex items-center justify-center">
      <UploadCard />
    </div>
  );
}